import "UnityEngine"

if not UnityEngine.GameObject or not  UnityEngine.UI then
  error("Click Make/All to generate lua wrap file")
end
--local itemInfo
--local slotGrid
function main()
  local itemInfo = GameObject.Find("BagPanel(Clone)/Background/DescriptionText"):GetComponent(UI.Text)
  local slotGrid = GameObject.Find("ItemGrid")
  local luaTest = GameObject.Find("LuaTest")
  itemInfo.text=string.format("")
end
main()
function CloseBagPanel()
	local bagPanel=GameObject.Find("BagPanel(Clone)")
	GameObject.Destroy(bagPanel)
end
